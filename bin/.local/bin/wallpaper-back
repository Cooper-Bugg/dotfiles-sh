#!/bin/bash
PREV=$(cat "$HOME/.cache/prev-wallpaper" 2>/dev/null)
[[ -z "$PREV" || ! -f "$PREV" ]] && exit 0
cp "$HOME/.cache/current-wallpaper" "$HOME/.cache/prev-wallpaper" 2>/dev/null
awww img "$PREV"
echo "$PREV" > "$HOME/.cache/current-wallpaper"
echo "$PREV" > "$HOME/.cache/next-wallpaper"
